const person = {
  name: "Estela", // replace it with your name
  age: 21 // replace it with your age
};
const { name, age } = person;

const hobbies = ["Drawing", "Cooking"];
const [hobby1, hobby2] = hobbies;
 //replace it with your hobbies

function printName(person) {
  console.log(person.name);
}
console.log(person.name); // "Estela"
console.log(hobby1);  // 21



